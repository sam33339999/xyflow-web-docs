---
title: 放下時刪除邊線
description: 當邊線找不到連接點時刪除它
---

在這個範例中，我們展示如何透過使用 [`onReconnect`、`onReconnectStart` 和 `onReconnectEnd` 處理器](/api-reference/react-flow#onReconnect)來刪除邊線。如果你拖曳現有邊線並將其放到畫布上，它會從 `edges` 陣列中被刪除。

<RemoteCodeViewer
  route="examples/edges/delete-edge-on-drop"
  framework="react"
/>
