---
title: 水平流程
description: 從左到右流動的圖表
---

圖表可以從左到右或從上到下，但你也可以建立混合的圖表。
要控制圖表的方向，你可以透過 [`sourcePosition` 和 `targetPosition`](/api-reference/types/node) 屬性調整節點的連接點位置。

<RemoteCodeViewer route="examples/layout/horizontal" framework="react" />
