---
title: 放下邊線時加入節點
description: 無論你在哪裡放下連接線都會出現新節點
---

你可以透過使用 `onConnectStart` 和 `onConnectEnd` 處理器，在將連接線放到畫布上時建立新節點。

<RemoteCodeViewer
  route="examples/nodes/add-node-on-edge-drop"
  framework="react"
/>
