---
title: 更新節點
description: 更新特定節點的資料欄位
---

只要你將新建立的 `nodes` 或 `edges` 陣列傳遞給 `ReactFlow`，就可以自由更新節點和邊線的屬性。

<Callout type="info">
  你必須在節點上建立一個新的 `data` 物件，以通知 React Flow 資料已變更。
</Callout>
<br />

<RemoteCodeViewer route="examples/nodes/update-node" framework="react" />
