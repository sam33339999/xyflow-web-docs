---
title: 連接限制
description: 使用 `isConnectable` 屬性限制連接點可擁有的連接數量
---

這是一個具有自訂連接點的自訂節點範例，可以使用 `isConnectable` 屬性來限制連接點可擁有的連接數量。
你可以使用布林值、數字（連接點應具有的最大連接數）或返回布林值的回呼函式，作為 CustomHandle 組件的 `isConnectable` 屬性參數。

<RemoteCodeViewer route="examples/nodes/connection-limit" framework="react" />
