---
title: 觸控裝置
description: 點擊兩個連接點以連接它們，並讓連接點在較小裝置上變大
---

你可以透過連續點擊兩個連接點來在觸控裝置上連接節點。在這個範例中，我們增加了連接點的大小，使其更容易點擊。你可以透過將 [`connectOnClick` 屬性](/api-reference/react-flow#interaction)設定為 `false` 來停用此行為。

<RemoteCodeViewer route="examples/interaction/touch-device" framework="react" />
