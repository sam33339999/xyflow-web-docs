---
description:
  配置視口對滾動事件的響應方式，允許自由、垂直或水平平移。
---

# PanOnScrollMode

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/system/src/types/general.ts#L166)

`PanOnScrollMode` 列舉控制當使用者滾動時視口的平移行為。選擇 `Free` 進行無限制平移，`Vertical` 僅限上下移動，或 `Horizontal` 僅限左右移動。

```ts
enum PanOnScrollMode {
  Free = 'free',
  Vertical = 'vertical',
  Horizontal = 'horizontal',
}
```
