---
description:
  'Connection 類型是兩個節點之間連線的基本描述。addEdge 工具函式可以用來將 Connection 升級為 Edge。'
---

# Connection

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/system/src/types/general.ts/#L29-L34)

`Connection` 類型是兩個節點之間 [`Edge`](/api-reference/types/edge) 的基本描述。[`addEdge`](/api-reference/utils/add-edge) 工具函式可以用來將 `Connection` 升級為 [`Edge`](/api-reference/types/edge)。

## 欄位

<APIDocs typeName="Connection" />
