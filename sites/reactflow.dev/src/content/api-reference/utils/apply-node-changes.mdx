---
description:
  "ReactFlow 元件上的各種事件可以產生 NodeChange，用於描述如何以某種方式更新流程圖中的節點。
  如果您不需要任何自訂行為，可以使用此工具函式取得這些變更的陣列並將它們套用到您的節點上。"
---

# applyNodeChanges()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/react/src/utils/changes.ts/#L140)

[`<ReactFlow />`](/api-reference/react-flow) 元件上的各種事件可以產生 [`NodeChange`](/api-reference/types/node-change)，用於描述如何以某種方式更新流程圖中的節點。如果您不需要任何自訂行為，可以使用此工具函式取得這些變更的陣列並將它們套用到您的節點上。

```js
import { useState, useCallback } from 'react';
import { ReactFlow, applyNodeChanges } from '@xyflow/react';

export default function Flow() {
  const [nodes, setNodes] = useState([]);
  const [edges, setEdges] = useState([]);
  const onNodesChange = useCallback(
    (changes) => {
      setNodes((oldNodes) => applyNodeChanges(changes, oldNodes));
    },
    [setNodes],
  );

  return (
    <ReactFlow nodes={nodes} edges={edges} onNodesChange={onNodesChange} />
  );
}
```

## Signature

<APIDocs functionName="applyNodeChanges" />

## Notes

- 如果您不需要任何自訂行為，[`useNodesState`](/api-reference/hooks/use-nodes-state) hook 會方便地為您封裝此工具函式和 React 的 `useState` hook，使用起來可能更簡單。
