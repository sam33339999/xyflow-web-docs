---
description: '此工具函式會返回給定邊界的視窗。
  您可以使用它在伺服器上預先計算給定節點集的視窗，或在不直接變更視窗的情況下計算給定邊界的視窗。'
---

import { Callout } from 'nextra/components';

# getViewportForBounds()

[Source on Github](https://github.com/xyflow/xyflow/blob/main/packages/system/src/utils/general.ts/#L170)

此工具函式會返回給定邊界的視窗。您可以使用它在伺服器上預先計算給定節點集的視窗，或在 _不直接變更_ 視窗的情況下計算給定邊界的視窗。

> [!NOTE]
>
> 此函式先前稱為 `getTransformForBounds`

```js
import { getViewportForBounds } from '@xyflow/react';

const { x, y, zoom } = getViewportForBounds(
  {
    x: 0,
    y: 0,
    width: 100,
    height: 100,
  },
  1200,
  800,
  0.5,
  2,
);
```

## Signature

<APIDocs functionName="getViewportForBounds" />

## Notes

- 這是一個相當底層的工具函式。您可能需要查看 [`fitView`](/api-reference/types/react-flow-instance#fitview) 或 [`fitBounds`](/api-reference/types/react-flow-instance#fitbounds) 方法以獲得更實用的 API。
