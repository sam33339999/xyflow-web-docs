---
description:
  '此工具函式用於告訴您哪些節點（如果有的話）作為連線的目標連接到給定的節點。'
---

# getOutgoers()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/system/src/utils/graph.ts/#L64)

此工具函式用於告訴您哪些節點（如果有的話）作為連線的 _目標_ 連接到給定的節點。

```ts
import { getOutgoers } from '@xyflow/react';

const nodes = [];
const edges = [];

const outgoers = getOutgoers(
  { id: '1', position: { x: 0, y: 0 }, data: { label: 'node' } },
  nodes,
  edges,
);
```

## Signature

<APIDocs functionName="getOutgoers" />
