---
description:
  '此工具函式用於告訴您哪些節點（如果有的話）作為連線的來源連接到給定的節點。'
---

# getIncomers()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/system/src/utils/graph.ts/#L91)

此工具函式用於告訴您哪些節點（如果有的話）作為連線的 _來源_ 連接到給定的節點。

```ts
import { getIncomers } from '@xyflow/react';

const nodes = [];
const edges = [];

const incomers = getIncomers(
  { id: '1', position: { x: 0, y: 0 }, data: { label: 'node' } },
  nodes,
  edges,
);
```

## Signature

<APIDocs functionName="getIncomers" />
