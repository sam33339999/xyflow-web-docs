---
description:
  "ReactFlow 元件上的各種事件可以產生 EdgeChange，用於描述如何以某種方式更新流程圖中的連線。
  如果您不需要任何自訂行為，可以使用此工具函式取得這些變更的陣列並將它們套用到您的連線上。"
---

# applyEdgeChanges()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/react/src/utils/changes.ts/#L167)

[`<ReactFlow />`](/api-reference/react-flow) 元件上的各種事件可以產生 [`EdgeChange`](/api-reference/types/edge-change)，用於描述如何以某種方式更新流程圖中的連線。如果您不需要任何自訂行為，可以使用此工具函式取得這些變更的陣列並將它們套用到您的連線上。

```js
import { useState, useCallback } from 'react';
import { ReactFlow, applyEdgeChanges } from '@xyflow/react';

export default function Flow() {
  const [nodes, setNodes] = useState([]);
  const [edges, setEdges] = useState([]);
  const onEdgesChange = useCallback(
    (changes) => {
      setEdges((oldEdges) => applyEdgeChanges(changes, oldEdges));
    },
    [setEdges],
  );

  return (
    <ReactFlow nodes={nodes} edges={edges} onEdgesChange={onEdgesChange} />
  );
}
```

## Signature

<APIDocs functionName="applyEdgeChanges" />

## Notes

- 如果您不需要任何自訂行為，[`useEdgesState`](/api-reference/hooks/use-edges-state) hook 會方便地為您封裝此工具函式和 React 的 `useState` hook，使用起來可能更簡單。
