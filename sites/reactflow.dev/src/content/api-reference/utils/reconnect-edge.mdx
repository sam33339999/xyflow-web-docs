---
description:
  '一個方便的工具函式，用於使用新屬性重新連接現有的 Edge。
  這會在您的連線陣列中搜尋具有相符 id 的連線，並使用您提供的連接更新其屬性。'
---

# reconnectEdge()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/system/src/utils/edges/general.ts)

一個方便的工具函式，用於使用新屬性更新現有的 [`Edge`](/api-reference/types/edge)。這會在您的連線陣列中搜尋具有相符 `id` 的連線，並使用您提供的連接更新其屬性。

```js
const onReconnect = useCallback(
  (oldEdge: Edge, newConnection: Connection) => setEdges((els) => reconnectEdge(oldEdge, newConnection, els)),
  []
);
```

## Signature

<APIDocs functionName="reconnectEdge" />
