---
description:
  '這個 hook 返回已連接的連線陣列。使用此 hook 的元件會在任何連線變更時重新渲染。'
created_at: 2025-01-15
---

# useNodeConnections()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/react/src/hooks/useNodeConnections.ts)

這個 hook 返回特定節點、連接點類型（'source'、'target'）或連接點 ID 的連接陣列。

```jsx
import { useNodeConnections } from '@xyflow/react';

export default function () {
  const connections = useNodeConnections({
    handleType: 'target',
    handleId: 'my-handle',
  });

  return (
    <div>There are currently {connections.length} incoming connections!</div>
  );
}
```

## 簽名

<APIDocs functionName="useNodeConnections" />
