---
title: 視窗 Portal 元件
sidebarTitle: '<ViewportPortal />'
description: '視窗 Portal 元件可用於將元件添加到渲染節點和連線的相同流程視窗中。當你想要渲染自己的元件，讓它們遵循與節點和連線相同的座標系統，並且也受縮放和平移影響時，這會很有用。'
created_at: 2024-02-21
---

# &lt;ViewportPortal />

[原始碼於 GitHub](https://github.com/xyflow/xyflow/blob/main/packages/react/src/components/ViewportPortal/index.tsx)

`<ViewportPortal />` 元件可用於將元件添加到渲染節點和連線的相同流程視窗中。
當你想要渲染自己的元件，讓它們遵循與節點和連線相同的座標系統，並且也受縮放和平移影響時，這會很有用。

```jsx
import React from 'react';
import { ViewportPortal } from '@xyflow/react';

export default function () {
  return (
    <ViewportPortal>
      <div
        style={{ transform: 'translate(100px, 100px)', position: 'absolute' }}
      >
        This div is positioned at [100, 100] on the flow.
      </div>
    </ViewportPortal>
  );
}
```

## Props

<APIDocs componentName="ViewportPortal" />
