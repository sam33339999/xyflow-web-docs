---
title: 新的連線範例
description:
  這裡有一批新的連線範例，包括如何沿著連線路徑動畫節點、如何建立臨時連線，以及每個連線事件的示範。
authors: [hayleigh]
date: '2024-08-14'
---

import { RemoteCodeViewer } from 'xy-shared/server';

這裡有一批新的連線範例，包括如何沿著連線路徑動畫節點、如何建立臨時連線，以及每個連線事件的示範。我們希望這一組新範例能幫助你提升你的流程技能！

<RemoteCodeViewer
  route="examples/edges/animating-edges"
  framework="react"

/>

這是已新增內容的細分：

- [二合一範例](/examples/edges/animating-edges)展示如何沿著連線路徑動畫 SVG 元素和其他節點。

- 一個新範例展示如何建立只附加到一個連接點的[臨時連線](/examples/edges/temporary-edges)。這些連線可以稍後重新拾起並連接。

- 我們有很多與連線相關的事件，所以我們建立了一個[小示範](/examples/interaction/connection-events)，展示每個連線和事件以及它們何時被觸發。
