---
description: 這三種變體以列舉形式匯出以方便使用。您可以匯入列舉並使用 BackgroundVariant.Lines，或直接使用原始字串值。
---

# BackgroundVariant

這三種變體以列舉形式匯出以方便使用。您可以匯入列舉並使用 `BackgroundVariant.Lines`，或直接使用原始字串值。

```ts
export enum BackgroundVariant {
  Lines = 'lines',
  Dots = 'dots',
  Cross = 'cross',
}
```
