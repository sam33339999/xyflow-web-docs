---
description: SvelteFlow 中在刪除節點或邊前觸發的回呼函式，允許阻止或修改刪除操作。
---

# OnBeforeDelete

[在 GitHub 上查看原始碼](https://github.com/xyflow/xyflow/blob/main/packages/svelte/src/lib/types/general.ts#L38)

`OnBeforeDelete` 類型是在刪除節點或邊前被呼叫的回呼函式。它可以透過回傳 `false` 或修改後的節點和邊集合來阻止刪除或修改要刪除的項目。

```ts
type OnBeforeDelete<
  NodeType extends NodeBase = NodeBase,
  EdgeType extends EdgeBase = EdgeBase,
> = ({
  nodes,
  edges,
}: {
  nodes: NodeType[];
  edges: EdgeType[];
}) => Promise<boolean | { nodes: NodeType[]; edges: EdgeType[] }>;
```

<APIDocs typeName="OnBeforeDelete" packageName="svelte" />
