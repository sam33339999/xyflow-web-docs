# MarkerType

Svelte Flow 內建兩種標記：`MarkerType.Arrow` 和 `MarkerType.ArrowClosed`。您可以透過設定 `markerStart`/ `markerEnd` [Edge](/api-reference/types/edge) 選項來使用這些標記。

```ts
export enum MarkerType {
  Arrow = 'arrow',
  ArrowClosed = 'arrowclosed',
}
```
