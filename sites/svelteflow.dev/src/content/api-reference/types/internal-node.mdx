---
title: InternalNode
description: 內部節點是使用者節點的擴展。它在內部使用，包含一些不直接暴露給使用者的額外資訊，例如 `positionAbsolute` 和 `handleBounds`。
---

# InternalNode\<NodeType>

內部節點是使用者節點的擴展。它在內部使用，包含一些不直接暴露給使用者的額外資訊，例如 `positionAbsolute` 和 `handleBounds`。

## 欄位

<APIDocs typeName="InternalNode" />

## 注意事項

- 可以使用 [`useInternalNode`](/api-reference/hooks/use-internal-node) hook 或 [`getInternalNode`](/api-reference/hooks/use-svelte-flow#get-internal-node) 來存取內部節點。
