---
description: SvelteFlow 中在重新連接邊之前觸發的回呼函式，允許阻止或修改重新連接操作。
---

# OnBeforeReconnect

[在 GitHub 上查看原始碼](https://github.com/xyflow/xyflow/blob/main/packages/svelte/src/lib/types/general.ts#L34)

`OnBeforeReconnect` 類型是在邊重新連接前被呼叫的回呼函式。它可以透過回傳 `false` 或修改後的連線來阻止重新連接或修改連線。

```ts
type OnBeforeReconnect<EdgeType extends EdgeBase = EdgeBase> = (
  oldEdge: EdgeType,
  newConnection: Connection,
) => Promise<boolean | Connection>;
```

<APIDocs typeName="OnBeforeReconnect" packageName="svelte" />
