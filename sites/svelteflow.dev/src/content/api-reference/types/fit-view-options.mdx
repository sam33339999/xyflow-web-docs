---
description: 在呼叫 fitView 時，可以使用這些選項來自訂行為。例如，duration 選項可用於在給定的時間內平滑地轉換視窗。
---

# FitViewOptions

在呼叫 `fitView` 時，可以使用這些選項來自訂行為。例如，`duration` 選項可用於在給定的時間內平滑地轉換視窗。

## 欄位

<APIDocs typeName="FitViewOptions" />
