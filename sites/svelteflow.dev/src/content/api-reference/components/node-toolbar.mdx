---
title: NodeToolbar 元件
sidebarTitle: '<NodeToolbar />'
description: 'NodeToolbar 元件可用於在節點的側邊顯示工具列，或顯示提示框等。'
---

# \<NodeToolbar />

[在 GitHub 上查看原始碼](https://github.com/xyflow/xyflow/blob/main/packages/svelte/src/lib/plugins/NodeToolbar/NodeToolbar.svelte)

NodeToolbar 元件可用於在節點的側邊顯示工具列，或顯示提示框等。

```svelte filename="CustomNode.svelte"
<script lang="ts">
	import { NodeToolbar, Handle, Position, type NodeProps } from '@xyflow/svelte';

	let { data } : NodeProps = $props();
</script>

<NodeToolbar>
	<button>delete</button>
	<button>copy</button>
	<button>expand</button>
</NodeToolbar>

<div>{data.label}</div>
<Handle type="target" position={Position.Left} />
<Handle type="source" position={Position.Right} />
```

## 屬性

`<NodeToolbar />` 元件的屬性類型導出為 `NodeToolbarProps`。此外，它擴展了 `<div />` 的屬性。

<APIDocs componentName="NodeToolbar" groupKeys="HTMLAttributes<HTMLDivElement>" />

## 注意事項

- 預設情況下，工具列只在節點被選中時才可見。如果選中了多個節點，
  它將不可見以避免工具列重疊或雜亂。你可以透過將 `isVisible` 屬性
  設定為 `true` 來覆寫此行為。
