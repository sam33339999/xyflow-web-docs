---
description:
  '此工具函式用於告訴你哪些節點（如果有的話）作為邊緣的來源節點，連接到給定的節點。'
---

# getIncomers()

[在 GitHub 上查看原始碼](https://github.com/xyflow/xyflow/blob/main/packages/system/src/utils/graph.ts/#L121-L137)

此工具函式用於告訴你哪些節點（如果有的話）作為邊緣的來源節點，連接到給定的節點。

```ts
import { getIncomers } from '@xyflow/svelte';

let nodes = $state.raw([]);
let edges = $state.raw([]);

const incomers = getIncomers(
  { id: '1', position: { x: 0, y: 0 }, data: { label: 'node' } },
  nodes.value,
  edges.value,
);
```

## Signature

<APIDocs functionName="getIncomers" />
